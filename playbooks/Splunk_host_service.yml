# Automation Playbook Generated by SIEMPLY
automation_playbook:

  - job:
      id: job_1753691480920
      name: "Splunk directory"

      targets:
        server_class: Splunk_HF

      execution_options:
        remote_user: root
        on_failure: stop

      tasks:
        - name: "Splunk LS command"
          command:
            cmd: "ls -lah /opt/"

  - job:
      id: job_1753691613426
      name: "Splunk Service Restart"

      targets:
        server_class: Splunk_HF

      execution_options:
        remote_user: root
        become: yes
        become_user: splunk
        on_failure: stop

      tasks:
        - name: "Restart Service"
          command:
            cmd: "/opt/splunkforwarder/bin/splunk restart"

  - job:
      id: job_1754491407379
      name: "Splunk Status"

      targets:
        server_class: Splunk_HF

      execution_options:
        remote_user: root
        become: yes
        become_user: splunk
        on_failure: stop

      tasks:
        - name: "Splunk Status"
          command:
            cmd: "/opt/splunkforwarder/bin/splunk status"
