# Automation Playbook Generated by SIEMPLY
automation_playbook:

  - job:
      id: job_1754491757366
      name: "Splunk Service stop"

      targets:
        server_class: splunk_aws

      execution_options:
        remote_user: root
        become: yes
        become_user: splunk
        on_failure: stop

      tasks:
        - name: "Splunk Stop Service"
          command:
            cmd: "/opt/splunk/bin/splunk stop"

  - job:
      id: job_1754491851666
      name: "Delete and Check"

      targets:
        server_class: splunk_aws

      execution_options:
        remote_user: root
        on_failure: stop

      tasks:
        - name: "Delete Splunkforwarder"
          command:
            cmd: "rm -rf /opt/splunk/"

        - name: "Check /opt/"
          command:
            cmd: "ls -lah /opt/"
