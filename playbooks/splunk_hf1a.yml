# Automation Playbook Generated by SIEMPLY
automation_playbook:

  - job:
      id: job_1753562484621
      name: "splunk host time"

      targets:
        hosts:
          - 192.168.100.3

      execution_options:
        remote_user: root
        on_failure: stop

      tasks:
        - name: "splunk timer"
          command:
            cmd: "for i in {1..120}; do echo 'Second: $i'; sleep 1; done"
          register: backup_result
