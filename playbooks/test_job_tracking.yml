# Automation Playbook Generated by SIEMPLY
automation_playbook:

  - job:
      id: job_test_1
      name: "Test Job 1"

      targets:
        hosts:
          - localhost

      execution_options:
        remote_user: root
        on_failure: stop

      tasks:
        - name: "Echo test"
          command:
            cmd: "echo 'Hello from SIEMply job tracking test!'"

        - name: "Date command"
          command:
            cmd: "date"

  - job:
      id: job_test_2
      name: "Test Job 2"

      targets:
        hosts:
          - localhost

      execution_options:
        remote_user: root
        on_failure: stop

      tasks:
        - name: "List directory"
          command:
            cmd: "ls -la /tmp"

        - name: "System info"
          command:
            cmd: "uname -a"
